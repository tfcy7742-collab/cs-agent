[pytest]
testpaths = tests
python_files = test_*.py
# -p no:cacheprovider：本项目的运行环境对 .pytest_cache 目录写入受限，
#   开着缓存插件会不停留下空目录（pytest-cache-files-*），关掉更干净。
# 不用 --basetemp：临时目录夹具在 conftest.py 里自建在工作区内。
addopts = -q --strict-markers -p no:cacheprovider
filterwarnings =
    ignore::DeprecationWarning
